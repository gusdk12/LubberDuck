<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lec.spring.repository.order.Order_ItemRepository">

    <sql id="SELECT_BASE">
        SELECT
            id "id"
             , order_id "order_id"
             , cocktail_id "cocktail_id"
             , quantity "quantity"
             , price "price"
             , id "id"
             , user_id "user_id"
             , number "number"
             , regdate "regdate"
             , id "id"
             , name "name"
             , img_url "img_url"
             , info "info"
             , price "price"
             , sequence "sequence"
            FROM ctt_order_item oi, ctt_order o, ctt_menu m
    </sql>

    <select id="findByOrder" resultType="com.lec.spring.domain.order.Order_item">
        <include refid="SELECT_BASE"/>
        WHERE oi.order_id = #{param1}
    </select>

</mapper>